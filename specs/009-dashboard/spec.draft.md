# 009. ダッシュボード(ドメイン別ビュー)

## ステータス

未着手(スケルトンのみ、Phase 3)

## 概要

チャットとは別の画面として、ドメイン別のビュー(関係グラフ、TODOバケット表示等)を提供する。`003-chat-ui-polish`で導入するgenerative UI(tool結果の専用コンポーネント表示)だけでは対応しきれなくなった規模・用途(検索・フィルタ付き一覧等)で必要になる想定。

## 前提・依存

- 複数ドメイン(論文・TODO・日次まとめ)が揃っていること
- `003-chat-ui-polish` のgenerative UIパターンで限界を感じた時点が着手のトリガー

## 詳細

着手時に詰める。判断材料は `wishlist/wishlist-design.md` Layer4を参照。
