# 006. 過去ログ(Claude webエクスポート)からの構造化抽出

## ステータス

未着手(スケルトンのみ)

## 概要

Claude.aiの「データのエクスポート」(設定 > プライバシー、アカウント全体をJSONで非同期取得)から、論文に関する過去の会話を見分け、雑多なテキストからPaperRecordの構造化フィールドと要約を抽出する。精度は高くない前提で割り切る。

## 前提・依存

- `005-eval-harness` があると、抽出精度(スキーマ妥当性率)を実データで検証できて都合がよい

## 詳細

着手時に詰める。判断材料は `wishlist/discussion/2026-08-14-discussion-log-and-repo-structure.md` の「Ingest経路とチャットアプリ化」節を参照。
